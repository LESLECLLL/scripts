using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SVERCHKI : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("crickets");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
